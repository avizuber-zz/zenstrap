'use strict';

describe('myApp.actionPanel module', function() {

  beforeEach(module('myApp.actionPanel'));

  describe('actionPanel controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var actionPanelCtrl = $controller('ActionPanelCtrl');
      expect(view2Ctrl).toBeDefined();
    }));

  });
});